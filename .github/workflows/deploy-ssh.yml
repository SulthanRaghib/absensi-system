name: 🚀 Advanced Deploy via SSH

on:
  push:
    branches:
      - master # Sesuaikan dengan branch utama Anda

jobs:
  deploy:
    name: 🚧 Build & Deploy to Server
    runs-on: ubuntu-latest

    steps:
      # 1. Ambil Kodingan Terbaru
      - name: 🚚 Checkout Code
        uses: actions/checkout@v4

      # 2. Setup PHP & Install Composer Dependencies
      # Penting: Composer install harus dilakukan SEBELUM npm run build
      # karena Filament membutuhkan file CSS dari folder vendor/
      - name: 🔵 Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.3"

      - name: 🧶 Install Composer Dependencies (Local)
        run: composer install --prefer-dist --no-ansi --no-interaction --no-progress --no-scripts

      # 3. Setup Node.js untuk Build Frontend (Vite/Tailwind)
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: 📦 Install NPM & Build Assets
        run: |
          npm install
          npm run build

      # 4. Siapkan SSH Key di Runner Sementara
      - name: 🔑 Configure SSH
        run: |
          mkdir -p ~/.ssh/
          # Menggunakan printf untuk menangani newline dengan lebih baik
          printf "%s\n" "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key

          # Debug: Cek format key (Header & Footer saja agar aman)
          echo "Checking Key Format..."
          head -n 1 ~/.ssh/deploy_key
          tail -n 1 ~/.ssh/deploy_key

      # DEBUGGING
      - name: 🔍 Debug Secrets (Check Empty)
        run: |
          if [ -z "${{ secrets.SSH_HOST }}" ]; then echo "❌ Error: Secret SSH_HOST is empty"; exit 1; fi
          if [ -z "${{ secrets.SSH_USER }}" ]; then echo "❌ Error: Secret SSH_USER is empty"; exit 1; fi
          if [ -z "${{ secrets.TARGET_DIR }}" ]; then echo "❌ Error: Secret TARGET_DIR is empty"; exit 1; fi
          if [ -z "${{ secrets.SSH_PRIVATE_KEY }}" ]; then echo "❌ Error: Secret SSH_PRIVATE_KEY is empty"; exit 1; fi
          echo "✅ All required secrets are present!"

      # 5. Kirim File menggunakan RSYNC (Cepat & Cerdas)
      - name: 📤 Rsync Deploy to Server
        run: |
          echo "🚀 Starting deployment to ${{ secrets.SSH_HOST }}..."

          # Test koneksi SSH dulu sebelum rsync
          ssh -p ${{ secrets.SSH_PORT }} -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "echo '✅ SSH Connection Successful'"

          rsync -avz --delete \
            -e "ssh -p ${{ secrets.SSH_PORT }} -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no" \
            --exclude='.git/' \
            --exclude='.github/' \
            --exclude='.env' \
            --exclude='.env.example' \
            --exclude='node_modules/' \
            --exclude='storage/framework/sessions/*' \
            --exclude='storage/framework/cache/*' \
            --exclude='storage/framework/views/*' \
            --exclude='storage/logs/*' \
            ./ ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.TARGET_DIR }}

      # 6. Eksekusi Perintah Laravel di Server (Remote Command)
      # Ini bagian 'Advanced'-nya: Migrate & Optimize otomatis!
      - name: 🚀 Run Remote Commands
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            cd ${{ secrets.TARGET_DIR }}

            # --- KONFIGURASI PHP DIRECTADMIN ---
            # Ini adalah path standar DirectAdmin untuk PHP 8.3
            PHP_BIN="/usr/local/php83/bin/php"

            # Cek versi untuk memastikan di Log GitHub (Harus 8.3)
            echo "🔍 Checking PHP Version..."
            $PHP_BIN -v

            # 1. Maintenance Mode
            $PHP_BIN artisan down --render="errors::503" || true

            # 2. Install Composer dengan PHP 8.3
            # Kita download composer.phar manual agar aman & menggunakan PHP_BIN
            echo "📦 Installing Dependencies..."
            curl -sS https://getcomposer.org/installer | $PHP_BIN
            $PHP_BIN composer.phar install --no-interaction --prefer-dist --optimize-autoloader --no-dev

            # 3. Jalankan Migrasi
            echo "Rx🚀 Running Migrations..."
            $PHP_BIN artisan migrate --force

            # 4. Optimasi Cache Laravel & Filament
            echo "🧹 Optimizing..."
            $PHP_BIN artisan optimize:clear
            $PHP_BIN artisan config:cache
            $PHP_BIN artisan route:cache
            $PHP_BIN artisan view:cache
            $PHP_BIN artisan filament:optimize

            # 5. Storage Link
            $PHP_BIN artisan storage:link

            # 6. Online Kembali
            $PHP_BIN artisan up

            # Bersihkan file composer sementara
            rm composer.phar

            echo "✅ Deployment Successfully Finished!"
